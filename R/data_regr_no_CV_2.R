#' Pre-trained Random Forest Model Without Coefficient of Variation (Part 2)
#'
#' This object represents the second part of a pre-trained random forest model that does not include the average coefficient of variation (CV) as a feature.
#'
#' @format A pre-trained random forest model that excludes average CV as a feature (Part 2).
#'
'child.nodeIDs.no.CV'
