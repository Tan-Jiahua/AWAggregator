#' Pre-trained Random Forest Model Without Coefficient of Variation (Part 4)
#'
#' This object represents the fourth part of a pre-trained random forest model that does not include the average coefficient of variation (CV) as a feature.
#'
#' @format A pre-trained random forest model that excludes average CV as a feature (Part 4).
#'
'split.values.no.CV.2'
