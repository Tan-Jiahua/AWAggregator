#' Pre-trained Random Forest Model Without Coefficient of Variation (Part 5)
#'
#' This object represents the fifth part of a pre-trained random forest model that does not include the average coefficient of variation (CV) as a feature.
#'
#' @format A pre-trained random forest model that excludes average CV as a feature (Part 5).
#'
'split.varIDs.no.CV'
